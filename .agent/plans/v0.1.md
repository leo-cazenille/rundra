# Rundra v0.1 implementation plan

Status: M0 complete at its acceptance gate; M1.4 complete and M1 in progress

Last updated: 2026-08-15

Authority: `docs/project_specs.md`, especially sections 5–22, 28–47, and 49–53

## Purpose and non-negotiable boundaries

This is the living, dependency-aware plan from the initial repository to the
v0.1 definition of done. It preserves these invariants throughout:

- `Run` is a logical framework object, never a scheduler job.
- `Task` is a logical execution, never a Slurm array index.
- experiment configuration contains portable scientific intent; target
  configuration contains site/backend details.
- every stochastic task has an explicit integer seed.
- local and remote execution use the same planner and orchestration service.
- each submitted Run uses a new isolated source snapshot; staging later Runs
  cannot mutate it.
- concrete Slurm, SSH, rsync, and Apptainer code depends on portable models and
  ports, never the reverse.
- human and JSON CLI renderers consume the same typed operation-result objects.
- normal tests use local or fake infrastructure; shoal tests are opt-in.
- no persistent remote daemon, speculative plugin framework, or second
  scheduler is part of v0.1.

## Current repository state

Observed on 2026-08-14:

- Git contains one initial commit (`630f60a`) with only `.gitignore` tracked.
- `AGENTS.md` and `docs/project_specs.md` exist as untracked working-tree files;
  they are treated as authoritative user-owned inputs and are not modified by
  this plan.
- `README.md` exists and is empty.
- There is no `pyproject.toml`, `uv.lock`, package, CLI, tests, examples, CI
  configuration, or prior planning convention.
- No M0–M6 implementation milestone was complete at planning time.

The only changes made while producing this plan are `.agent/PLANS.md` and this
file. M0 remains entirely unstarted.

### Current state at the M0 acceptance gate (2026-08-15)

- The locked Python 3.12 package contains portable domain/configuration models,
  pure deterministic planning, narrow infrastructure protocols, and test-only
  scripted fakes. No concrete adapter, store, workspace mutation, or execution
  service exists.
- `validate`, `plan`, and `targets` are non-executing CLI operations with human
  and version-1 JSON renderers over the same typed result values.
- Checked minimal YAML and JSON-contract examples live under `examples/minimal/`
  and `docs/schemas/`.
- The M0 gate validation passes with 208 default-CI tests and no external
  infrastructure. M1 and every later milestone remain unstarted.

### Current state after M1.1 (2026-08-15)

- A frozen version-1 `RunRecord` preserves the normalized experiment, exact
  task configs, portable resources/states, target, timestamps, artifacts, and
  available optional provenance without inventing missing values.
- The `RunStore` boundary and `JsonRunStore` provide strict load/list behavior,
  collision-safe creation, validated lifecycle updates, same-directory durable
  temporary writes, and atomic replacement.
- The checked persisted contract is `docs/schemas/run-record-v1.json`.
- No staging, run workspace, container invocation, or execution adapter exists;
  those remain M1.2+ work.

### Current state after M1.2 (2026-08-15)

- `LocalStager` allocates collision-safe per-Run workspaces, copies current
  source plus the byte-exact effective config, and returns backend-neutral
  semantic paths for sealed and mutable areas.
- Default and experiment-configured exclusions prevent transient files and an
  in-tree `.rundra` workspace from entering the snapshot. Source symlinks are
  dereferenced to keep the snapshot isolated from later target changes.
- Source/input write bits are removed before staging completes; runtime,
  output, logs, and metadata remain writable. Local fetch rejects unsafe paths
  and symlinks and atomically replaces matching retrieved output files.
- No container command construction, scheduler/transport execution, or CLI run
  operation exists; those remain M1.3+ work.

### Current state after M1.3 (2026-08-15)

- `ApptainerRuntime` validates and constructs immutable, shell-free `exec`
  commands without executing the runtime. It applies clean-environment and
  no-evaluation controls, optional NVIDIA passthrough, explicit read-only or
  read-write bind mounts, and an absolute container working directory.
- Experiment environment values travel through the runtime-native inherited
  environment prefix: `APPTAINERENV_` by default and `SINGULARITYENV_` for a
  configured `singularity` executable. Unsafe names, NULs, option-like images,
  relative container paths, ambiguous bind paths, and duplicate bind
  destinations fail explicitly.
- Capability validation performs executable discovery without running a
  container or a version command. Container execution, orchestration, and the
  CLI run lifecycle remain M1.4+ work.

### Current state after M1.4 (2026-08-15)

- `LocalTransport` executes immutable argv without a shell and captures exact
  command context, UTF-8 stdout/stderr, timestamps, and non-zero exit status.
  `LocalScheduler` synchronously executes exactly one `ExecutionUnit`, assigns
  an opaque native reference, and retains a terminal portable observation.
- `OrchestrationService` validates one prospective plan against its experiment,
  creates the durable record before capability checks, stages through `Stager`,
  builds through `ContainerRuntime`, submits through `Scheduler`, and persists
  each observed lifecycle phase through `RunStore`.
- The service records stdout/stderr as separate Task artifacts and treats the
  versioned RunRecord artifact collection as the manifest. It fetches requested
  partial output after non-zero exit and preserves computation success when
  retrieval independently fails.
- The application service is implemented, but no execution CLI, asynchronous
  local run, provenance collector, native runtime, or multi-Task execution has
  been added; those remain later milestones.

## Dependency map and implementation sequence

```text
M0 models + contracts + fakes
  |
  v
M1 complete local vertical path
  |
  v
M2 SSH + isolated rsync staging
  |
  v
M3 Slurm execution adapter
  |
  v
M4 real shoal CPU/GPU vertical path
  |
  v
M5 multi-task model + Slurm arrays
  |
  v
M6 contracts, hardening, release
```

This retains the specified M0–M6 order. Within M0, only interfaces required by
M1 are fixed. M2 deliberately proves transport/staging separately before M3
adds scheduler behavior. M4 supplies real-backend evidence before M5 commits to
array mapping and per-task reconciliation details.

## Proposed minimal package layout

Create only modules needed by the current milestone; the eventual v0.1 layout
is expected to be:

```text
src/rundra/
├── __init__.py
├── domain/
│   ├── models.py          # ExperimentSpec, Run, Task, resources, artifacts
│   └── states.py          # portable state values and transition rules
├── config/
│   ├── experiments.py     # YAML parsing, validation, normalization
│   └── targets.py         # target parsing and backend selection data
├── orchestration/
│   ├── models.py          # ExecutionPlan/Unit and operation result objects
│   ├── planner.py         # portable spec + invocation -> immutable plan
│   └── service.py         # lifecycle coordination through ports
├── ports.py               # narrow Transport/Scheduler/Stager/Runtime protocols
├── adapters/
│   ├── local.py           # local transport/scheduler/stager implementations
│   ├── ssh.py
│   ├── rsync.py
│   ├── slurm.py
│   └── apptainer.py
├── persistence/
│   └── json_store.py
├── provenance/
│   └── git.py
└── cli/
    ├── main.py
    └── render.py

tests/
├── fakes.py
├── unit/
├── integration/
├── contracts/
└── system/                # marked, opt-in shoal tests

examples/minimal/
docs/schemas/
```

This is intentionally smaller than one package per backend interface. Split an
adapter into a package only when its implementation becomes unwieldy. Test
fakes stay in `tests/fakes.py` unless a supported public testing API becomes a
real need. A backend registry, dynamic discovery, abstract base-class hierarchy,
generic storage-role ontology, and public Python API are premature.

## Minimum architecture and abstraction limits

### Domain models

v0.1 needs immutable or mutation-controlled typed values for:
`ExperimentSpec`, command template, container request, `ResourceRequest`,
`Target`, `Run`, `Task`, `Artifact`, `RunRecord`, identifiers, timestamps, and
portable states. Config content is opaque YAML data but its exact effective
bytes/content are copied into the run record. Domain types contain no backend
objects or Slurm/SSH terminology; explicit native resource options remain a
namespaced mapping at the portable boundary.

Extensibility required now: additive schema evolution via version fields,
multiple tasks per Run, backend-native metadata kept separately, and absent
optional provenance represented explicitly. Deferred: arbitrary parameter
axes, DAGs, lineage, structured scientific metrics, a universal resource
ontology, and user-defined domain plugins.

### Planning and orchestration

v0.1 needs a pure planner that expands config plus seed(s), validates target
capabilities, resolves workspace-relative paths, and produces an inspectable
`ExecutionPlan` containing logical `ExecutionUnit` values. The orchestration
service consumes that plan, advances durable lifecycle state, and calls ports in
the order validate → persist CREATED → stage → submit/execute → reconcile →
fetch. Planning performs no submission and should avoid filesystem mutation
apart from reading/validating inputs.

Extensibility required now: execution strategy and backend metadata are explicit
plan fields, so Slurm arrays can group units later without changing Tasks.
Deferred: general workflow graphs, retry policy frameworks, policy engines,
adaptive campaigns, packing strategies, and event buses.

### `Transport`

v0.1 needs `check()` and `run(command, cwd, env)` returning a typed
`CommandResult` (argv, exit code, stdout, stderr, and timing). `Command` is an
argv tuple; SSH is responsible for the unavoidable remote-shell serialization
and quoting. Local and SSH transports share the port.

Extensibility required now: callers cannot depend on SSH text or authentication
details. Deferred: streaming/interactive sessions, connection pools, SFTP,
jump-host modeling beyond normal OpenSSH configuration, and capability
negotiation protocols.

### `Scheduler`

v0.1 needs `submit(normalized unit/group)`, `query(job references)`,
`cancel(job references)`, and enough accounting/log metadata to reconcile exit
state. It accepts an execution description—not `ExperimentSpec`—and returns
typed scheduler references and observations with portable and native states.
Waiting is orchestration-level polling over `query`, keeping scheduler adapters
free of workflow policy. `LocalScheduler` and `SlurmScheduler` exercise the same
port.

Extensibility required now: one Run may map to one or more scheduler references,
and many Tasks may map through an explicit backend mapping. Deferred until a
second real scheduler: a supposedly universal job-description language,
scheduler feature negotiation, queue/accounting normalization beyond required
fields, packed execution, and declaring the port stable.

### `Stager`

v0.1 needs operations that create a unique run workspace, copy the current
source tree and effective inputs into it, seal the source snapshot before
submission, and fetch requested outputs/metadata idempotently. Local and rsync
implementations return typed workspace/artifact information. The port expresses
semantic paths needed by orchestration, not rsync flags or a universal storage
API.

Extensibility required now: local and remote path locations differ, and fetch is
separate from computation state. Deferred: Globus/object storage, content
addressing, deduplication, retention policy, generalized `$HOME`/`$SCRATCH`
roles, and `SharedFilesystemStager` unless shoal implementation proves it removes
real duplication.

### `ContainerRuntime`

v0.1 needs validation and pure argv construction from a normalized application
command, image, GPU flag, binds, environment, and workspace paths. Apptainer
owns `--nv` and bind syntax; Slurm never constructs container arguments. A tiny
native/no-container implementation may be used only if M1 confirms the
specification's “local/container execution” requires CI without Apptainer; it
must implement the same port and not become a bypass around orchestration.

Extensibility required now: runtime selection belongs to the target and runtime
metadata is preserved. Deferred: image building/pulling, digest caches,
Singularity as a separate adapter, Docker/OCI abstraction, and runtime plugins.

### Persistence

v0.1 needs a `RunStore`-shaped boundary with create, load, update, and list over
one schema-versioned JSON record per Run under a configurable user data root.
Writes use a temporary file in the same directory, flush/fsync as justified,
then `os.replace`; creation must fail on ID collision. Immutable snapshot/input
areas are separated from explicitly mutable runtime, output, logs, and metadata
locations. Retrieval outcome is recorded independently of computation outcome.

Extensibility required now: orchestration depends on store behavior, not path
layout, and unknown schema versions fail explicitly. Deferred: relational or
distributed databases, migrations before a second schema, distributed locks,
event sourcing, and multi-host writers. Evidence from concurrency tests will
decide whether a small local lock is needed.

### CLI and structured output

v0.1 needs the specified commands and `--json` on every useful operation.
Handlers call application services and receive typed result/error objects;
human and JSON renderers are the only divergent paths. JSON objects carry a
format/schema version, stable identifiers/states/error codes, deterministic
keys/collection ordering where relevant, and appropriate process exit codes.

Extensibility required now: the application service is independent from
`argparse` so a future Python/MCP/REST adapter can reuse it. Deferred: MCP,
REST, a stable public Python SDK, shell completion, rich terminal UI, and generic
command plugin discovery.

## Decision log

| ID | Choice | Reason and alternatives | Stability |
|---|---|---|---|
| D001 | Canonical commands are immutable argv tuples with an optional explicit environment mapping and working directory. | Safe local execution and testable provenance. Shell strings and command-builder class hierarchies were rejected; SSH/Slurm may serialize argv only inside their adapters. | Stable invariant. |
| D002 | Parse versioned YAML into manually validated frozen dataclasses; use a small YAML library because Python has no standard YAML parser. Preserve effective config separately as opaque YAML-compatible data/content. | Gives precise errors and keeps the domain independent of validation libraries. A large model framework/JSON-Schema-first runtime is unnecessary; handwritten YAML parsing is unsafe and wasteful. Exact dependency is finalized in M0 and documented. | Stable shape; parser dependency provisional. |
| D003 | Run IDs are `run_` plus a lowercase UUID4 hex value; task IDs are deterministic zero-padded ordinals scoped to the Run (`task_000000`, ...). IDs are immutable value types and scheduler IDs are separate. | Standard library, collision-resistant, filesystem/JSON safe; avoids adding ULID. Time-sortable IDs and seed-derived task IDs were rejected because ordering can come from timestamps and seeds may repeat in future. | Stable for v0.1; format becomes public once documented. |
| D004 | Persist one strict versioned JSON `RunRecord` per Run in a configurable local data directory. Updates write and fsync a same-directory temporary file before `os.replace`; creation atomically links a complete temporary file and fails on collision. | Survives client exit, avoids partial JSON and accidental overwrite, is inspectable, and avoids premature SQLite. Unknown fields/versions fail explicitly. In-place JSON and a database were rejected. | Implemented in M1.1; multi-process update locking remains provisional until M6 concurrency testing. |
| D005 | Use the specification's nine portable states plus validated transition functions; preserve native observations separately. Computation state and retrieval state are distinct fields. | Prevents backend leakage and avoids reporting fetch failure as compute failure. An unrestricted mutable status string was rejected. Aggregation/UNKNOWN precedence is finalized with M5 evidence. | State names stable; transition/aggregation table provisional through M5. |
| D006 | Define four narrow structural protocols (`Transport`, `Scheduler`, `Stager`, `ContainerRuntime`) plus a store boundary, with explicit request/result values. No backend registry or inheritance framework. | Enables fakes and local/remote composition while avoiding speculative plugins. Concrete-only orchestration and broad base classes were rejected. | Protocol existence stable; method signatures provisional until M4. |
| D007 | `FakeTransport`, `FakeScheduler`, `FakeStager`, and a recording runtime live in test support and expose scripted outcomes plus call histories. Contract tests run against fakes and real local adapters where applicable. | Makes orchestration deterministic without cluster access. Mocking subprocess everywhere would couple tests to implementation. | Stable testing approach. |
| D008 | CLI handlers return common typed success/error results; separate human and JSON renderers serialize those same values. | Guarantees semantic parity and testability. Independent human/JSON command paths were rejected. | Stable invariant. |
| D009 | `ExecutionPlan`/`ExecutionUnit` form the portable handoff from scientific intent to adapters; scheduler grouping is a later backend strategy with an explicit Task mapping. | Keeps `Run`/`Task` distinct from job/array concepts and makes `plan` a true safety boundary. Passing `ExperimentSpec` directly to Slurm was rejected. | Stable boundary; exact fields provisional through M3. |
| D010 | The first package uses standard-library `argparse`, dataclasses, pathlib, subprocess, and JSON; dependencies are added only for YAML and demonstrably useful test/tooling support. | Keeps installation portable. A CLI/model framework is not justified yet. | Provisional; revisit only with concrete friction. |
| D011 | Target backend selection is a closed, explicit factory over v0.1 types, not dynamic plugins. Unknown backend types are structured configuration errors. | Auditable and sufficient for local/shoal. Entry points and plugin discovery are deferred until external backend distribution exists. | Provisional beyond v0.1. |
| D012 | Async durability is record-based: all remote scheduler references and paths needed by later CLI processes are persisted immediately after submission. No daemon owns a Run. | Directly supports the disconnected-client scenario. A remote service/background monitor was rejected. | Stable invariant. |
| D013 | Normalize portable memory to integer bytes and walltime to `datetime.timedelta` in `ResourceRequest`; human-readable YAML forms are parsed only at the M0.3 boundary. | Keeps arithmetic and validation unambiguous without leaking scheduler syntax. Keeping raw strings in the domain or adding unit wrapper hierarchies was rejected. | Stable for v0.1. |
| D014 | Represent each Task's effective opaque config as a `ConfigSnapshot` containing its source path and exact text content. | Preserves what ran without interpreting application-specific YAML; parsed schema data and application-specific config models were rejected. | Stable shape; serialization details deferred to M1 persistence. |
| D015 | Track result transfer independently with `NOT_REQUESTED`, `PENDING`, `SUCCEEDED`, and retryable `FAILED` retrieval states. | Implements D005's computation/transfer separation and permits retry after transfer failure without changing computation state. More detailed transfer phases are not evidenced yet. | Provisional through M1 fetch implementation. |
| D016 | Use PyYAML `SafeLoader` with an added duplicate-mapping-key rejection rule; keep `types-PyYAML` as development-only typing support. | Python has no standard YAML parser, PyYAML is small and mature, and safe loading avoids executable constructors. Handwritten YAML and a validation/model framework were rejected. | Stable dependency choice for v0.1. |
| D017 | Both experiment and target documents require top-level `version: 1`. Target v1 accepts only role-appropriate local/SSH, local/Slurm, local/rsync, and Apptainer selections; SSH `host` is the only backend option in the initial target schema. | Makes incompatible versions and backend configuration explicit while implementing only the concrete v0.1 path. An unversioned target file, arbitrary backend options, and plugin discovery were rejected. | Stable schema version; exact target fields provisional through M4 evidence. |
| D018 | Configuration failures use `ConfigError` with stable `code`, `message`, `source`, and tuple `path` fields. M0.5 operation errors and M0.6 JSON rendering will adapt these values rather than duplicate parsing logic. | Gives callers actionable structure now without prematurely defining the public CLI error envelope. Raw `KeyError`, parser exceptions, and formatted-string-only errors were rejected. | Stable internal boundary; JSON representation deferred to M0.6. |
| D019 | Experiment memory accepts positive integer `B`/`KiB`/`MiB`/`GiB`/`TiB` forms and walltime accepts positive `HH:MM:SS` with two-digit minutes/seconds; normalized domain values remain bytes and `timedelta`. | These forms cover the specification examples with unambiguous binary sizes and duration semantics. Decimal-unit aliases and scheduler-specific duration syntax were rejected until a concrete need exists. | Stable for schema v1. |
| D020 | Version-1 loaders reject common credential-bearing field names in command environments, native options, and target backend sections; authentication still comes only from external mechanisms. Semantic secret detection is not attempted inside argv or opaque scientific YAML, so users must never place credentials there. | A denylist provides defense in depth but cannot prove arbitrary strings are not secrets; interpreting opaque scientific configuration or inventing a credential-reference system would violate scope. M0.6 must document the prohibition and M1 persistence must not add credential sources. | Stable prohibition; detection list may expand without changing valid non-secret schema. |
| D021 | Seed ranges use inclusive `START:STOP`; a v0.1 plan requires a nonempty unique integer seed set and assigns zero-padded Task IDs by deterministic ordinal. `ExecutionPlan` is prospective and contains no Run ID. | Matches the CLI's `0:9` examples, preserves an explicit seed for every unit, and keeps planning free of random identity or submission side effects. Implicit seeds, half-open ranges, and seed-derived IDs were rejected. | Stable through v0.1; duplicate-seed campaigns may be reconsidered only with a second distinguishing axis. |
| D022 | Initial ports exchange frozen semantic request/result values. `Transport.run` accepts the canonical `Command` directly because D001 already places cwd/environment on that value; scheduler submission accepts `ExecutionUnit` groups and returns an explicit Task-to-native mapping. | Avoids two conflicting sources for cwd/environment and prevents Scheduler from consuming scientific `ExperimentSpec`. Separate `run(command, cwd, env)` arguments and a generic job-description hierarchy were rejected. | Protocol boundaries stable; exact signatures remain provisional through M4 as D006 requires. |
| D023 | Public CLI JSON uses a root `format_version: 1`, `operation`, and `ok`; success payloads are operation-specific and failures contain `code`, `message`, and `details`. Human and JSON renderers consume the same `OperationResult`. | Resolves the previously unversioned examples and gives agents a deterministic compatibility boundary. Independent render paths and unversioned envelopes were rejected. | Stable public M0 contract; backward-incompatible changes require a new format version. |
| D024 | `StagedWorkspace` exposes semantic source/input/config/runtime/output/log/metadata paths. `LocalStager` copies and permission-seals source/input, dereferences source symlinks, leaves explicit runtime areas writable, and atomically fetches safe relative output patterns. | Preserves snapshot isolation and a portable orchestration boundary without declaring the physical local layout public. Preserving symlinks would let external changes mutate an existing Run; copying into one mutable tree would blur inputs and outputs. | Semantic path roles stable for v0.1; default exclusions and physical layout remain implementation details. |
| D025 | `ApptainerRuntime` purely builds shell-free `exec --cleanenv --no-eval` argv with explicit `BindMount` access modes, optional `--nv`, and `--cwd`; command environment uses the configured runtime's inherited environment prefix. Its capability check discovers the executable without running it. | Keeps Apptainer syntax inside its adapter, preserves environment values without comma-delimited argument encoding or shell evaluation, and allows command construction for a remote target. Running a version probe would violate the pure M1.3 boundary. | Argument shape is provisional through M4 target evidence; the backend boundary and no-shell invariant are stable. |
| D026 | `LocalTransport` overlays explicit command environment on the inherited host environment and captures output without a shell; `LocalScheduler` executes one unit synchronously and returns its retained `CommandResult` through an optional field on `SchedulerObservation`. | A typed optional result avoids a local-only retrieval side channel while allowing remote schedulers to omit direct process output. A detached local process and daemon were rejected for M1.4. | Synchronous one-Task local behavior is stable for M1; async semantics and resource enforcement remain deferred. |
| D027 | `OrchestrationService` reproduces a one-Task plan before allocating a Run, then coordinates capability checks, staging, container construction, scheduling, log persistence, reconciliation, and fetch only through ports/store. Fixed semantic container mounts map sealed source/input read-only and output/runtime read-write. `RunRecord.artifacts` is the M1 manifest. | Keeps local and future remote paths on one application boundary, prevents executing a command that disagrees with recorded intent, and preserves failed-task partial output plus independent retrieval status. A CLI-owned flow and a second manifest schema were rejected. | Single-Task lifecycle stable through M1; polling/async and multi-Task aggregation remain provisional for M3/M5. |

## Deferred decisions

Do not decide these without implementation evidence:

- whether shoal benefits from a distinct `SharedFilesystemStager` rather than
  rsync plus configured shared paths;
- exact `squeue`/`sacct` fallback and reconciliation precedence for jobs that
  leave the queue before accounting becomes visible;
- polling intervals, timeouts, and retry policy beyond explicit safe defaults;
- whether local asynchronous `submit` is supported in v0.1 and, if so, how a
  detached process is reaped without a daemon (remote asynchronous submit is
  mandatory);
- container checksum caching policy and whether checksum collection is opt-in;
- whether atomic JSON replacement alone is sufficient or a local advisory lock
  is needed;
- final per-task aggregate-state precedence for mixed outcomes;
- exact JSON field sets beyond the first documented contract fixtures;
- a second scheduler, transport, staging technology, Python API, MCP/REST,
  policy engine, parameter sweep model, lineage, metrics, and backend plugins.

## Milestone checklist

- [ ] M0 — repository skeleton and core models
- [ ] M1 — local experiment execution
- [ ] M2 — remote transport and staging
- [ ] M3 — Slurm backend
- [ ] M4 — shoal vertical slice
- [ ] M5 — replicated experiments
- [ ] M6 — hardening and v0.1 release

No milestone was pre-existing or complete when this plan was created.

## M0 — repository skeleton and core models

**Goal:** Establish a runnable Python 3.12 project, portable data/contracts, and
deterministic fake-driven test foundation without executing experiments.

**Prerequisites:** Authoritative specification and this approved plan.

**Checkpoints and deliverables:**

- [x] M0.1 Add `pyproject.toml`, committed `uv.lock`, `src` package, CLI entry
  point, pytest/ruff/mypy configuration, and a smoke test. Pin supported Python
  to 3.12 and document the developer commands.
- [x] M0.2 Implement value types for command, IDs, resources, artifacts,
  `ExperimentSpec`, `Target`, `Run`, `Task`, states, and explicit transition
  validation. Domain modules import no adapter implementations.
- [x] M0.3 Implement version-1 experiment and target YAML loaders with strict
  unknown/invalid-field errors, opaque scientific config loading, duration and
  memory normalization, explicit native-option namespacing, and actionable
  structured errors.
- [x] M0.4 Implement pure seed expansion/task construction and the initial
  inspectable `ExecutionPlan`/`ExecutionUnit` model. `plan` creates no Run and
  performs no submission.
- [x] M0.5 Define only the narrow ports listed above, operation result/error
  values, fake adapters with call histories/scripted outcomes, and interface
  contract tests.
- [x] M0.6 Document initial schema examples and CLI `validate`, `plan`, and
  `targets` JSON contracts. Implement those non-executing commands from common
  result objects.

**Likely files/modules:** `pyproject.toml`, `uv.lock`, `src/rundra/domain/*`,
`config/*`, `orchestration/models.py`, `orchestration/planner.py`, `ports.py`,
`cli/*`, `tests/fakes.py`, `tests/unit/*`, `tests/contracts/*`,
`docs/schemas/*`, `README.md`, and examples containing no claims of execution.

**Public interfaces introduced:** experiment schema v1; target schema v1;
Run/Task ID formats; portable state names; `validate`, non-submitting `plan`,
and `targets`; JSON success/error envelope v1. Python internals remain unstable.

**Tests:** valid/invalid/unknown-version schemas; target/experiment separation;
resource normalization and unsupported resources; integer seed validation,
single/range expansion, deterministic Task IDs; command placeholder validation;
state transition table; JSON snapshots/contracts; fake call histories; proof
that planner does not call adapters.

**Validation:** `uv sync`; `uv run pytest`; `uv run ruff check .`;
`uv run ruff format --check .`; `uv run mypy src`; invoke `validate`, `plan`, and
`targets` in human and JSON modes against checked-in examples.

**Acceptance criteria:** clean checkout bootstraps with uv; all checks pass;
schema errors are structured/actionable; fixed input produces an equivalent
normalized plan and deterministic tasks; no concrete backend appears in domain
imports; fake adapters can script success, failure, and cancellation states.

**Risks/questions:** exact YAML library; placeholder grammar and duplicate seed
policy; portable memory/duration representation; target capability declaration;
how much of RunRecord belongs in M0 versus M1. Resolve with smallest version-1
schema consistent with examples, and update the specification if its public
contract is refined.

**Explicitly out of scope:** execution, filesystem workspaces, persistence,
Apptainer invocation, SSH, rsync, Slurm, dynamic plugins, sweeps, and declaring
any backend protocol stable.

## M1 — local experiment execution

**Goal:** Prove the complete orchestration model locally with an isolated source
snapshot, durable provenance, artifacts, and the same CLI/result path intended
for remote execution.

**Prerequisites:** M0.

**Checkpoints and deliverables:**

- [x] M1.1 Implement schema-versioned `RunRecord` serialization and atomic
  `JsonRunStore`; persist every lifecycle transition and separate computation
  and retrieval outcomes.
- [x] M1.2 Implement `LocalStager` with collision-safe Run layout, source/input
  copying, exclusions, immutable-after-start rules, explicit mutable locations,
  and idempotent local fetch.
- [x] M1.3 Implement pure `ApptainerRuntime` validation/argv construction for
  image, binds, environment, and GPU flag; add an execution-safe no-container
  path only if required by the agreed local schema.
- [x] M1.4 Implement local transport/scheduler behavior and orchestration for
  one Task: create → stage → execute → record → manifest → fetch, including
  non-zero exit and partial outputs.
- [ ] M1.5 Implement `run`, `status`, `list`, `logs`, `fetch`, `inspect`, and
  meaningful exit codes using common result objects. Keep `submit` unavailable
  or explicitly capability-rejected until its durable semantics are proven.
- [ ] M1.6 Add Git provenance capture (commit, branch if useful, dirty flag,
  patch when safely available) with graceful non-Git behavior; preserve the
  effective spec/config and source snapshot identity.
- [ ] M1.7 Add and run the minimal local example twice with the same seed and
  assert byte-for-byte or documented semantic reproducibility of its raw result.

**Likely files/modules:** `persistence/json_store.py`, `adapters/local.py`,
`adapters/apptainer.py`, `orchestration/service.py`, `provenance/git.py`, CLI
modules, `examples/minimal/*`, tests, schema docs, README/spec clarifications.

**Public interfaces changed:** local target behavior; RunRecord v1; artifact
manifest v1; lifecycle command JSON; local workspace/config defaults; `run`,
`status`, `list`, `logs`, `fetch`, `inspect` semantics.

**Tests:** RunRecord round trips and unknown versions; atomic update/collision;
allowed/forbidden transitions; staging isolation and exclusion; attempts to
mutate sealed input/source; runtime argv/quoting/GPU/binds/env; local success,
failure, missing artifact, partial fetch, repeat fetch, non-Git provenance;
CLI JSON and exit-code contracts; deterministic seed propagation/results.

**Validation:** baseline checks; minimal local example; fixed-seed repeat; inspect
the record and artifact manifest; run failure example and retrieve its logs.

**Acceptance criteria:** `rundr run ... --target local` uses planner and
ports, not a CLI special case; a later process can inspect the Run by ID; source
snapshots do not change when the project is edited; success/failure, stdout,
stderr, exit code, artifacts, config, and seed are accurately persisted; JSON
matches documented contracts.

**Risks/questions:** local container availability; practical filesystem
immutability (permissions versus orchestration invariant); copying large trees;
local async semantics; avoiding a false reproducibility claim when external
environment changes. Record limitations explicitly.

**Explicitly out of scope:** SSH, rsync, Slurm, remote async execution, arrays,
multi-seed Runs, container building/pulling, and generalized storage roles.

## M2 — remote transport and staging

**Goal:** Safely create and retrieve an isolated remote workspace over standard
OpenSSH/rsync without introducing scheduler behavior.

**Prerequisites:** M1's workspace, store, command, artifact, and orchestration
contracts.

**Checkpoints and deliverables:**

- [ ] M2.1 Implement OpenSSH `SSHTransport` over argument-array subprocess calls,
  honoring user SSH config/agent/host verification and returning typed results.
- [ ] M2.2 Centralize and exhaustively test remote argv-to-shell quoting at the
  single unavoidable serialization boundary; redact sensitive environment/data
  from diagnostic output.
- [ ] M2.3 Implement remote workspace creation with validated run IDs and paths;
  reject unsafe workspace roots and path traversal.
- [ ] M2.4 Implement `RsyncStager` upload of uncommitted/non-Git working trees,
  effective input/config, default/configured exclusions, and sealing of a unique
  source snapshot.
- [ ] M2.5 Implement independent, idempotent result/log/metadata retrieval with
  explicit destination and retrieval state. A successful computation marker is
  not changed by transfer failure.
- [ ] M2.6 Add fake-SSH/fake-rsync integration flows using executable shims or
  recording transports; exercise remote commands and round trips without a
  network or Slurm.

**Likely files/modules:** `adapters/ssh.py`, `adapters/rsync.py`, target config,
orchestration service, persistence fields, tests/unit, tests/integration, and
remote staging documentation.

**Public interfaces changed:** SSH/rsync target fields and validation; target
connectivity/capability errors; remote workspace/retrieval metadata in plan and
RunRecord. No credentials enter schemas or records.

**Tests:** exact subprocess argv; spaces/quotes/metacharacters/newlines and
hostile YAML values; host aliases; failure categorization; path containment;
dirty and non-Git staging; exclusion behavior; two Runs from changing source
remain isolated; fake round-trip/repeat fetch; interrupted upload/fetch;
credential redaction.

**Validation:** baseline checks; fake transport/staging integration suite;
optional developer-owned SSH/rsync smoke test only when explicitly configured
and never in default CI.

**Acceptance criteria:** transport/staging tests require no cluster; current
working tree is copied to a new remote Run path without Git push; subsequent
source edits cannot affect that snapshot; retrieval is repeatable and its
failures are distinct; no `shell=True`, disabled host checking, or logged secret.

**Risks/questions:** remote POSIX shell is unavoidable for SSH command execution;
rsync availability/version and symlink semantics; atomic sealing over NFS;
partial upload cleanup; `~` expansion without unsafe client interpolation.

**Explicitly out of scope:** Slurm commands, scheduler polling, compute-node
execution, inbound connections, persistent remote agent, Globus/SFTP/object
storage, and automatic remote dependency installation.

## M3 — Slurm backend

**Goal:** Implement testable Slurm submission and lifecycle reconciliation over
the transport port, independent of shoal-specific validation.

**Prerequisites:** M2 remote command/staging path and M1 durable lifecycle.

**Checkpoints and deliverables:**

- [ ] M3.1 Define the minimal normalized scheduler submission unit/group and
  `SchedulerObservation`; finalize only protocol fields proven by local+Slurm.
- [ ] M3.2 Generate inspectable sbatch scripts independently of submission,
  translating portable CPU/GPU/memory/walltime fields and explicit
  `resources.native.slurm` options without silent override.
- [ ] M3.3 Submit via parsable Slurm output, capture scheduler job IDs, and
  persist references immediately so a new client process can continue.
- [ ] M3.4 Implement `squeue`/`sacct` querying and native-to-portable state/exit
  mapping with explicit UNKNOWN/accounting-delay behavior and native metadata.
- [ ] M3.5 Implement orchestration-level waiting/timeouts and async `submit`;
  implement idempotent/graceful `cancel` and reconcile cancellation races.
- [ ] M3.6 Normalize stdout/stderr locations, expose logs without requiring
  native filenames, and preserve scheduler metadata and partial failure data.
- [ ] M3.7 Add scripted Slurm command-generation/parsing and fake orchestration
  integration tests for success, failure, timeout, submission failure,
  disappearance, cancellation, and disconnected-client continuation.

**Likely files/modules:** `adapters/slurm.py`, scheduler values in
`orchestration/models.py`, service/store updates, CLI `submit/status/logs/cancel`,
tests and Slurm schema/behavior documentation.

**Public interfaces changed:** Slurm target/native resource fields; scheduler
metadata in plan/RunRecord/JSON; async `submit`; status, logs, cancel; scheduler
failure codes. Generated script is inspectable but not necessarily a stable
public schema.

**Tests:** golden script/argv tests for CPU/GPU and hostile values; unsupported
resources; native-option allow/deny rules; parsable-output parsing; comprehensive
state mapping; accounting lag; non-zero/timeout/cancel; duplicate cancel;
persistence reload after submit; fake end-to-end remote flow. No real Slurm.

**Validation:** baseline checks; all fake lifecycle flows; inspect generated
scripts manually/security review; confirm no normal test invokes installed Slurm.

**Acceptance criteria:** Slurm text parsing is contained in the adapter; job IDs
are separate from Run/Task IDs; scheduler references survive client exit;
portable/native states and exit codes are accurate; GPU allocation and
Apptainer GPU invocation remain independent; cancel/log operations work by Run
ID in fake integration.

**Risks/questions:** `sacct` availability/lag and version differences; safe
sbatch directive allowlist; terminal-state race reconciliation; output naming;
scheduler timeout versus experiment failure; avoiding native options that evade
site policy.

**Explicitly out of scope:** real shoal claims, Slurm arrays, multi-seed grouping,
PBS/LSF, advanced dependencies/reservations/requeue, accounting analytics, and
generic scheduler feature negotiation.

## M4 — shoal vertical slice

**Goal:** Validate and refine the single-task reference path on fishvision/shoal
for CPU and GPU workloads inside Apptainer.

**Prerequisites:** M3 plus explicit user access/configuration for shoal system
testing. Missing access blocks system validation, not unit-test progress.

**Checkpoints and deliverables:**

- [ ] M4.1 Add documented, non-secret shoal target example and an opt-in system
  test harness guarded by markers/environment checks and resource-conscious
  defaults.
- [ ] M4.2 Run preflight/`plan` for SSH, rsync, workspace, Slurm, Apptainer,
  requested resources, image, and shared filesystem; errors identify the failed
  layer and corrective action.
- [ ] M4.3 Validate one CPU Run end to end from modified/uncommitted source:
  stage, submit, wait, logs, raw results, fetch, record, and provenance.
- [ ] M4.4 Validate one minimal GPU Run with both Slurm GPU allocation and
  independent Apptainer NVIDIA enablement; keep test bounded and opt-in.
- [ ] M4.5 Validate experiment non-zero exit and one safe remote infrastructure
  failure; retrieve logs/partial artifacts and preserve accurate state.
- [ ] M4.6 Feed real observations back into only the necessary port fields,
  target defaults, parsers, documentation, and fake fixtures; document Slurm,
  Apptainer, SSH, rsync, and NFS versions observed.

**Likely files/modules:** shoal target examples, `tests/system/*`, adapter fixes,
capability/preflight code, provenance, examples, README/system-test docs, and
possibly specification changes if real behavior contradicts an assumed public
contract.

**Public interfaces changed:** documented shoal target/configuration and
capabilities; real preflight/error behavior; container/source/scheduler
provenance fields. Avoid gratuitous schema changes.

**Tests:** opt-in SSH/rsync connectivity; CPU; GPU; config/seed propagation;
stdout/stderr; source dirty patch; container identity when practical; fetch;
failure and partial artifacts. Convert every reproducible adapter issue into a
default-CI unit/fake regression test.

**Validation:** baseline checks first; then documented opt-in shoal system test
commands; inspect scheduler allocation, container GPU visibility, immutable
remote source, records, artifacts, and cleanup guidance. Record actual evidence
and job IDs privately where appropriate, never credentials.

**Acceptance criteria:** the single command local-source→shoal→results workflow
works for CPU and GPU; no manual SSH/rsync/sbatch/log hunting is required;
uncommitted source and exact config/seed execute from an isolated snapshot;
failed work remains inspectable; default CI still requires no cluster.

**Risks/questions:** cluster availability/permissions/QOS; target-specific GPU
GRES details; NFS consistency; Apptainer/image availability; large checksum
cost; transient scheduler/accounting behavior. Never bypass site restrictions
to make a test pass.

**Explicitly out of scope:** arrays/multi-seed, production resource policy,
automatic image build/install, cluster administration, daemon deployment, and
claims about non-shoal clusters.

## M5 — replicated experiments

**Goal:** Support one logical Run containing deterministic seed Tasks, optimized
on Slurm with an explicit and inspectable job-array mapping.

**Prerequisites:** M4's proven single-task shoal path and M0's logical task model.

**Checkpoints and deliverables:**

- [ ] M5.1 Finalize seed-range CLI semantics, duplicate handling, stable Task
  ordering/IDs, per-task workspace/output/log locations, and RunRecord schema
  fields; preserve one effective config across Tasks.
- [ ] M5.2 Add a planner execution strategy that groups compatible Tasks for a
  Slurm array while retaining logical `Task` objects. Record Task ID ↔ seed ↔
  array index explicitly.
- [ ] M5.3 Generate array scripts/task manifests without interpolating config or
  seed data into unsafe shell; validate scheduler array bounds and resource
  uniformity.
- [ ] M5.4 Reconcile array/job accounting into per-task portable/native states,
  exits, logs, and artifacts; define and document aggregate Run-state precedence.
- [ ] M5.5 Support partial failure, cancellation of active array work, per-task
  log selection, repeat/partial fetch, and concise human/JSON status summaries.
- [ ] M5.6 Prove deterministic multi-seed behavior in fake/default integration
  tests, then validate a small opt-in shoal array with at least one controlled
  partial failure case if site policy permits.

**Likely files/modules:** domain/task record fields, planner strategy,
`adapters/slurm.py`, service/store, CLI seed parsing/status/logs/fetch, contract
schemas, unit/integration/system tests, examples/docs.

**Public interfaces changed:** `--seeds` syntax; task list/status/log selection;
task mapping and aggregate fields in RunRecord/JSON; partial-failure behavior.

**Tests:** range boundaries and invalid/duplicate seeds; deterministic IDs and
ordering; proof Tasks are not array indices; array script/manifest generation;
mapping round trip; mixed queued/running/succeeded/failed/cancelled aggregation;
per-task logs/artifacts; partial fetch/failure; cancellation races; CLI JSON
contracts; small opt-in shoal array.

**Validation:** baseline checks; deterministic seed propagation; fake lifecycle
matrix; minimal local multi-task run through the common orchestration path where
supported; opt-in shoal array; inspect mapping and every task's result.

**Acceptance criteria:** one Run contains N stable Tasks for N requested seeds;
Slurm may use one array job but mappings remain explicit; each Task is
inspectable and has accurate state/logs/exit/artifacts; mixed outcomes are not
collapsed; aggregate status is documented and deterministic.

**Risks/questions:** Slurm array accounting syntax/version differences; large
status payloads; scheduler array-size limits; aggregate state semantics;
cancellation/partial retrieval races; tasks with heterogeneous future
resources. v0.1 arrays require compatible homogeneous execution units.

**Explicitly out of scope:** generic parameters/sweeps, heterogeneous task
packing, adaptive scheduling, array throttling policy beyond an explicit native
option, automatic retries, and cross-Run campaigns.

## M6 — hardening and v0.1 release

**Goal:** Close all public contracts, failure/security gaps, documentation, and
required scenarios; remove abstractions not justified by the working path.

**Prerequisites:** M0–M5 feature acceptance.

**Checkpoints and deliverables:**

- [ ] M6.1 Audit every CLI operation (`validate`, `plan`, `run`, `submit`,
  `status`, `list`, `logs`, `fetch`, `cancel`, `inspect`, `targets`) for common
  result objects, deterministic `--json`, exit codes, and actionable errors.
- [ ] M6.2 Complete target capability validation and `plan` safety output,
  including resources, native options, execution strategy, staging, and no
  side effects/submission.
- [ ] M6.3 Stress RunStore atomicity, collision handling, interrupted writes,
  concurrent reads/status/fetch/cancel, unknown versions, and idempotency;
  introduce narrowly scoped locking only if tests prove it necessary.
- [ ] M6.4 Perform security review of subprocess argv, the two unavoidable shell
  formats (SSH remote command and sbatch script), paths, symlinks, environment,
  native options, log redaction, credentials, and host verification.
- [ ] M6.5 Complete user/agent documentation, versioned schema examples,
  installation/target setup, local/shoal examples, troubleshooting, artifact
  and provenance semantics, and opt-in system-test instructions. Reconcile
  `docs/project_specs.md` with actual public behavior.
- [ ] M6.6 Run the complete default validation matrix and all v0.1 scenarios;
  triage every skip/failure and record exact release evidence below.
- [ ] M6.7 Review architecture against two implemented execution paths; remove
  dead/premature layers, mark Python internals unstable, freeze/document v0.1
  schemas and CLI semantics, and create the v0.1 release checklist/changelog.

**Likely files/modules:** all public/adapter boundaries, tests, `README.md`,
`docs/project_specs.md`, schema/example docs, system-test guide, packaging/release
metadata, and this plan.

**Public interfaces changed:** only deliberate documented v0.1 finalization;
schema/format versions and compatibility behavior; complete CLI/error contracts.

**Tests:** complete unit/integration/contract suites; every failure category;
JSON schema/snapshot validation; disconnected client; idempotent fetch/cancel;
concurrency/interruption; hostile input/path/quoting/redaction; reproducibility;
all opt-in shoal system scenarios.

**Validation:** `uv sync --locked`; baseline tests/lint/format/type checks;
package build/install smoke test in a clean environment; minimal local example
and fixed-seed comparison; every CLI JSON document parsed/contract-validated;
opt-in shoal checklist below; documentation link/example verification.

**Acceptance criteria:** all section 53 definition-of-done items and section 44
scenarios pass with recorded evidence; default tests need no real infrastructure;
public docs match behavior; no secret or policy bypass is present; run management
survives process exit; implementation contains no unjustified generic framework.

**Risks/questions:** access needed for final shoal evidence; stabilizing schemas
too early; platform variance in OpenSSH/rsync/Slurm/Apptainer; honest scope of
reproducibility; packaging name may still be provisional.

**Explicitly out of scope:** PBS/LSF/Kubernetes, Globus/object stores, MCP/REST,
distributed database, persistent daemon, full resource-policy engine, parameter
sweeps, lineage, structured metrics, automatic analysis, and v1-stable Python
APIs.

## Cross-milestone testing strategy

Testing is an architectural constraint:

- `FakeTransport` records argv/cwd/env and scripts connectivity/command results.
- `FakeScheduler` scripts observation sequences such as
  SUBMITTED→QUEUED→RUNNING→SUCCEEDED, RUNNING→FAILED, timeouts, and cancellation;
  it returns native metadata without scheduler prose leaking upward.
- `FakeStager` records unique workspace/source/input/fetch operations and can
  fail each phase independently.
- A recording runtime captures normalized input and returns known argv.
- Contract tests specify each port's observable behavior and run against fakes;
  local adapters also run applicable contracts. Concrete remote adapters use
  command generation/parsing tests plus fake executable/transport integration.
- State-transition tests cover valid/invalid paths and keep retrieval status
  independent. M5 adds deterministic aggregate-state tables.
- RunRecord tests round-trip every known/unknown/optional field, reject future
  incompatible versions, and simulate interrupted writes.
- CLI tests invoke the entry point, parse JSON rather than matching prose, and
  assert schema, stdout/stderr separation, deterministic ordering, and exit code.
- System tests carry a dedicated `system`/`shoal` marker and explicit opt-in;
  ordinary CI selects them out and performs no network or Slurm calls.

Every remote bug found in M4/M5 must first gain a default-CI regression fixture
when it can be reproduced without the cluster.

## Highest-risk areas

1. Safe SSH and sbatch shell serialization: argv cannot cross those boundaries
   directly, so quoting/path/native-option validation needs focused adversarial
   tests and one ownership point per format.
2. Durable asynchronous reconciliation: `squeue` disappearance, delayed/absent
   `sacct`, cancellation races, and retrieval failures must not corrupt the
   portable state or lose scheduler references.
3. Snapshot immutability and provenance: rsync must capture dirty/non-Git trees
   into a Run-specific location, handle partial transfers, and avoid symlink/path
   escape while keeping logs/output writable.
4. Slurm array accounting: explicit Task/seed/index mapping, mixed terminal
   states, per-task logs, and site/version-specific accounting formats require
   real M4 evidence before M5 finalization.
5. Public contract timing: JSON/schema choices become expensive to change, so
   M0 documents only necessary envelopes and M6 freezes exact v0.1 fields after
   both execution paths work.
6. Real-environment validation: CPU/GPU acceptance depends on authorized shoal
   access, valid image/resources, and cluster availability; never substitute a
   fake test for recorded system evidence.

## Ambiguities and specification tensions to resolve incrementally

- M1 requires both “Apptainer runtime” and a minimal local example, while the
  broader local scenario says “local/container execution.” It is unclear whether
  local execution without Apptainer is required. Decide schema/behavior in M1;
  keep any native runtime tiny and on the common port.
- v0.1 requires asynchronous Runs and all CLI commands, but does not explicitly
  state whether `submit` must work for the local target. Remote/Slurm async is
  mandatory; local async should be capability-rejected or implemented only with
  sound post-process semantics.
- “Run directories immutable once execution begins” coexists with runtime
  state/output/log writes. The plan interprets this as immutable source/input
  and explicitly mutable runtime/output/log/metadata subtrees, matching
  `AGENTS.md`.
- The specification lists a portable `Target` domain concept while demanding
  target/site separation. The plan treats `Target` as portable configuration
  composition data with backend-specific config values, never scientific
  experiment content.
- `UNKNOWN` is listed as a portable Run state but transition/aggregation rules
  are unspecified. M0 defines conservative transitions; M5 finalizes aggregate
  precedence from real scheduler behavior.
- The specification suggests versions “where appropriate” but public JSON
  examples omit a format version. M0 should add/document one consistently or
  explicitly document why the envelope is versioned elsewhere; this is a
  specification clarification, not a silent change.
- The spec requires source/container identity “when practical.” M1 should record
  unavailable values explicitly; M4 supplies evidence for checksum cost and
  whether caching is justified.

No direct conflict between `AGENTS.md` and `docs/project_specs.md` was found.
`AGENTS.md` reinforces the same boundaries and adds the explicit rule that only
designated runtime/output locations may change after execution starts.

## Progress log

| Date | Status | Evidence / next action |
|---|---|---|
| 2026-08-14 | Planning complete; M0–M6 not started. | Read repository rules and full specification; inspected all existing files and Git state; created planning convention and this plan. Next: execute M0.1 only. |
| 2026-08-15 | M0.1 complete; M0 remains in progress. | Added the Python 3.12 uv/Hatch scaffold, lockfile, help-only CLI, smoke test, and truthful README. TDD evidence: the test first failed because `rundra` was absent, then passed after the minimal implementation; a later mutation of the console-script mapping also failed the test before restoration. Validation: `uv sync --locked` passed; `uv run pytest` passed (1 test); `uv run ruff check .` passed; `uv run ruff format --check .` passed (10 files); `uv run mypy src` passed (4 source files); and `uv run rundr --help` exited 0. Independent review found no M0.2 leakage; its console-script coverage finding was fixed and mutation-tested. Next: M0.2 only. |
| 2026-08-15 | M0.2 complete; M0 remains in progress. | Re-audited M0.1 with the installed uv, then implemented M0.2 in isolated worktree `feature/m0.2-domain-models` through repeated RED→GREEN cycles. Added frozen portable command, ID, resource, artifact, experiment, target, config snapshot, task, and run values; explicit integer seeds; nine execution states; independent Run retrieval state; and explicit transition validation. Adversarial review exposed incomplete runtime type/deep-immutability checks, so regression tests and structural validation were added across every public value. Final validation: `uv sync --locked` passed; `uv run pytest` passed (113 tests); Ruff lint/format passed (15 files); mypy passed (7 source files); installed CLI help passed; and `git diff --check` passed. Independent re-review found no blocking issues and confirmed no YAML/schema, planning, port, adapter, persistence, or backend work leaked from M0.3+. Acceptance audit: all M0.2 value types exist, every dataclass is frozen, state transitions are exhaustively tested, domain imports contain no adapter implementations, and malformed nested values cannot undermine immutability. Next: M0.3 only. |
| 2026-08-15 | M0.3 complete; M0 remains in progress. | Implemented M0.3 on isolated branch `feature/m0.3-config-loaders` through RED→GREEN cycles: PyYAML SafeLoader-compatible single-document parsing with full-path duplicate-key detection; structured path-aware errors; strict experiment/target version-1 schemas; bounded memory/walltime normalization; explicitly namespaced scalar native resources; role-checked target backends; credential-field rejection; and syntax-validated opaque config snapshots preserving exact UTF-8 text, newline style, and EOF. Adversarial review exposed newline translation, exception leaks, recursive-alias and merge-key compatibility regressions, incomplete credential-name checks, and truncated duplicate paths; each received a failing regression test before its fix. Final validation: `uv sync --locked` passed; `uv run pytest` passed (175 tests); Ruff lint/format passed (23 files); mypy passed (13 source files); installed CLI help passed; and `git diff --check` passed. Independent re-review found no blocking issues and confirmed no M0.4 leakage. Acceptance audit: both schemas require version 1 and reject unknown/invalid fields; errors expose code/message/source/path; memory and walltime normalize deterministically; native options remain namespaced; target and scientific configuration remain separate; opaque scientific YAML is not interpreted or reserialized; and no planning, adapters, persistence, or execution were introduced. Next: M0.4 only. |
| 2026-08-15 | M0.4 complete; M0 remained in progress. | Added pure inclusive seed expansion, explicit placeholder validation, deterministic logical Task construction, and frozen `ExecutionPlan`/`ExecutionUnit` values. Repeated fixed inputs compare equal, Task IDs are ordinal and stable, and prospective plans contain no Run or scheduler identity. Focused RED→GREEN tests cover missing/conflicting/invalid seeds, placeholder failures, duplicate seeds, and the Run/Task boundary. Next: M0.5 only. |
| 2026-08-15 | M0.5 complete; M0 remained in progress. | Added only the four D006 structural ports and frozen semantic values, plus renderer-independent operation success/error values. Test-only fake transport, scheduler, stager, and recording runtime expose scripted results/exceptions and complete call histories; contract tests exercise successful commands/submission/staging, failures, portable/native observations, and cancellation without infrastructure. Implementation evidence refined `Transport.run` to accept D001's complete `Command` rather than duplicate cwd/environment parameters; recorded as D022. Next: M0.6 only. |
| 2026-08-15 | M0.6 and the M0 acceptance gate complete; M1 not started. | Added strict non-executing `validate`, `plan`, and `targets` operations; deterministic human/JSON renderers over the same typed results; checked version-1 success/error contracts; and minimal experiment/config/target examples. Resolved I003 by documenting `format_version: 1` in the specification and D023, and corrected the target example to include schema version 1. Gate review found that YAML non-finite floats would make native options impossible to emit as standards-compliant JSON, so RED→GREEN checks now reject them at configuration/domain boundaries and the renderer enforces strict JSON. Full gate validation: `uv sync --locked`; 208 pytest tests; Ruff lint and format; strict mypy over 20 source files; installed human and JSON CLI invocations for all three commands; and `git diff --check` all pass. Acceptance audit confirms actionable schema errors, deterministic plans/tasks, no concrete-backend imports in domain/orchestration, scripted fake success/failure/cancellation, and no M1 execution, persistence, workspace, provenance, or concrete-adapter work. Stop at M0; next task is M1.1 only. |
| 2026-08-15 | M1.1 complete; M1 remains in progress. | Added frozen version-1 `RunRecord`, strict lossless JSON conversion, a structural `RunStore`, and atomic `JsonRunStore` creation/update/load/list. RED began with the missing record module; 15 focused tests now cover complete and absent provenance round trips, checked schema parity, unknown versions/fields, malformed nested data, duplicate JSON keys, immutable collection copies, collision safety, missing Runs, independent computation/retrieval transitions, immutable definitions, and preservation after failed replacement. Full validation: `uv sync --locked`; 223 pytest tests; Ruff lint and format; strict mypy over 26 source files; package/CLI smoke tests; and `git diff --check` pass. No staging, workspace creation, execution, or concrete adapter was introduced. Next: M1.2 only. |
| 2026-08-15 | M1.2 complete; M1 remains in progress. | Added `LocalStager` and refined `StagedWorkspace` with semantic sealed/mutable paths. RED began with the missing adapter package; eight focused adapter tests cover exact source/config copies, default/configured exclusions, permission sealing with executable-bit preservation, mutable runtime areas, post-stage isolation, independent Runs, collision rejection, in-source workspace recursion, symlink dereferencing, unsafe request rejection, partial-copy cleanup, atomic repeat fetch, artifact metadata, traversal rejection, output symlinks, and protected Run destinations. Full validation: `uv sync --locked`; 231 pytest tests; Ruff lint and format; strict mypy over 28 source files; CLI/package smoke tests; and `git diff --check` pass. No runtime argv, experiment execution, scheduler, or run CLI was introduced. Next: M1.3 only. |
| 2026-08-15 | M1.3 complete; M1 remains in progress. | Added pure `ApptainerRuntime` capability discovery and shell-free command construction, plus portable `BindMount` access modes. RED began with the missing adapter and bind model; focused tests cover exact CPU/GPU argv, Apptainer and Singularity environment inheritance, bind modes, working directories, hostile literal arguments, malformed environment/bind/image values, duplicate destinations, defensive copies, capability failures, and executable validation. Full validation: locked dependency sync; 251 pytest tests; Ruff lint and format over 53 files; strict mypy over 29 source files; package/CLI smoke tests; and `git diff --check` pass. No process or container execution, scheduler/transport adapter, orchestration, or run CLI was introduced. Next: M1.4 only. |
| 2026-08-15 | M1.4 complete; M1 remains in progress. | Added shell-free `LocalTransport`, synchronous one-Task `LocalScheduler`, optional captured command results on scheduler observations, and a port-driven `OrchestrationService`. RED began with missing execution adapters/service; eight adapter, seven infrastructure-free integration, and one port-contract test cover cwd/environment/argv safety, start errors, reference retention, non-zero mapping, exact plan reconciliation, durable capability/staging failures, successful lifecycle persistence, separate logs, artifact manifest, partial output after exit 7, computation/retrieval independence, and optional-result consistency. Full validation: locked dependency sync; 267 pytest tests; Ruff lint and format over 57 files; strict mypy over 30 source files; package/CLI/schema smoke tests; architecture/diff hygiene; all pass without Apptainer or external infrastructure. No lifecycle CLI, async local process, provenance capture, native runtime, multi-Task execution, or M2 backend was introduced. Next: M1.5 only. |

## Discovered issues

| ID | Status | Issue | Resolution/next action |
|---|---|---|---|
| I001 | Resolved before M0.1 | `AGENTS.md` and `docs/project_specs.md` were untracked when planning began. | The repository owner committed both authoritative inputs in `0c5e949`; subsequent implementation preserved and followed them. |
| I002 | Resolved in M0.1 | Empty README and no Python/uv configuration existed. | Added the locked Python 3.12 scaffold and truthful status documentation; all configured checks pass. |
| I003 | Resolved in M0.6 | JSON examples lacked an explicit envelope version although versioning is encouraged. | Added and contract-tested the D023 version-1 envelope; documented it in `docs/project_specs.md` and `docs/schemas/`. |
| I004 | Partially resolved in M1.4 | Local non-container and local async semantics were underspecified. | M1.4 deliberately implements synchronous, container-required execution and no daemon. Keep `submit` unavailable in M1.5; add a native runtime only if M1.7 evidence requires it. |
| I005 | Open | Exact shoal runtime versions/capabilities and accessible test image are not recorded. | Discover through authorized opt-in M4 preflight; do not hard-code assumptions in core models. |
| I006 | Resolved | The execution environment initially did not provide `uv` on `PATH`. | uv 0.12.5 is now installed. This sandbox exposes the normal user cache as read-only, so agent validation sets `UV_CACHE_DIR` to a temporary directory; documented contributor commands remain unchanged. |
| I007 | Resolved in M0.2 | Frozen dataclasses alone did not prevent callers from supplying mutable or incorrectly typed nested values at runtime. | Added explicit structural validation and defensive copies for every M0.2 public value, with adversarial regression tests; no validation framework dependency was needed. |
| I008 | Resolved in M0.3 | The specification's target example omits a version even though M0.3 requires a version-1 target loader and section 40 recommends versioning target configuration. | Require top-level `version: 1` for target files, matching experiment files and preventing silent reinterpretation; record this as D017 and document examples in M0.6. |
| I009 | Resolved as an explicit M0.3 boundary | No parser can determine whether arbitrary argv or opaque scientific YAML string values contain secrets, so literal semantic enforcement of the credential prohibition is impossible without interpreting application data. | Reject common credential-bearing field names as defense in depth, rely only on external authentication, prohibit credentials in all remaining opaque values, and document this contract in M0.6 per D020. Do not add an unevidenced credential store/reference system. |
| I010 | Deferred to M1 with an explicit M0 boundary | The architecture overview says planning validates target capabilities, but M0 has no concrete runtime capability data beyond strict role/backend schema validation. | M0 verifies that a named, schema-valid target exists and exposes its requested resources/native options. M1 adapters must add evidence-based capability checks before execution; do not invent a generic capability-negotiation model in M0. |

## v0.1 acceptance criteria

All must be true before marking this plan complete:

- [ ] Minimal local example completes through the common orchestration path.
- [ ] Repeating it with a fixed seed meets the documented reproducibility check.
- [ ] Modified/uncommitted source stages to an isolated shoal snapshot.
- [ ] Single-seed shoal CPU and GPU experiments complete in Apptainer.
- [ ] One multi-seed Run maps stable Tasks explicitly to a Slurm array.
- [ ] Async status/log/fetch/cancel work from a new client process by Run ID.
- [x] Non-zero experiment exit preserves state, exit code, logs, and partial
  retrievable artifacts.
- [x] Computation success and retrieval success remain independently accurate.
- [ ] Effective spec/config, seeds, source/container/target/resources/scheduler
  provenance, timestamps, exit status, and artifacts are persisted when
  available; missing optional values are not fabricated.
- [ ] Every programmatically useful CLI command has contract-tested `--json`
  from the same internal result object as human output.
- [ ] Default unit/integration tests require no SSH, Slurm, rsync peer, or
  Apptainer installation.
- [ ] Tests, lint, formatting, typing, package smoke test, documentation, and all
  authorized opt-in shoal system checks pass with evidence recorded.

## Final v0.1 system-test checklist (opt-in, never ordinary CI)

Preflight:

- [ ] Authorized SSH config/agent connects to `fishvision` with host checking.
- [ ] Remote workspace is safe/writable and visible on intended compute nodes.
- [ ] rsync, Slurm commands, Apptainer, CPU partition, GPU allocation mechanism,
  account/QOS, and test image are available without bypassing policy.

Scenarios:

- [ ] Local single-seed CPU run and fixed-seed repeat.
- [ ] Shoal single-seed CPU run from a dirty working tree.
- [ ] Shoal single-seed GPU run with verified allocation and container GPU view.
- [ ] Shoal multi-seed array with mapping/per-task result verification.
- [x] Experiment non-zero exit with logs and partial fetch.
- [ ] Async cancellation with final scheduler/framework reconciliation.
- [ ] Disconnected-client submit followed by status/logs/fetch in new processes.
- [ ] Repeated fetch and cancel are safe; two concurrent Runs remain isolated.

Evidence review:

- [ ] RunRecord/provenance/artifact manifests contain only accurate available
  data and no credentials.
- [ ] Human/JSON results agree and JSON validates against documented contracts.
- [ ] Generated remote commands/scripts are inspectable, safely quoted, and
  contain explicit native options.
- [ ] Raw results remain distinct from any derived analysis output.
- [ ] Remote failures and result-transfer failures remain distinguishable.
- [ ] System-test resource usage and cleanup are documented.

## Exact recommended next Codex task

Implement **M1.5 only**: expose synchronous `run` plus persisted `status`, `list`,
`logs`, `fetch`, and `inspect` operations through common typed results and the
existing human/JSON renderer boundary. Add meaningful process exit codes and
version-1 JSON contract fixtures. Keep `submit` explicitly unavailable because
local async semantics are not implemented. Do not begin M1.6 provenance or
M1.7 reproducibility work.
